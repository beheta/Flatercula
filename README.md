

## Flatercula is a simple script that lets you control Linux using natural language. It uses Ollama as its backend.

# How to use

run this commands:

```
python -m venv flatercula
. flatercula/bin/activate
git clone https://github.com/beheta/Flatercula.git
cd Flatercula
sudo bash install.sh
```

Have fun!
